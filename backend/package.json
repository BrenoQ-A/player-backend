{
  "name": "player-sinalizacao-backend",
  "version": "2.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node entrypoint.js",
    "check": "node --check entrypoint.js && node --check staging.js && node --check server.js && node --check storage.js && node --check catalog.js && node --check devices.js && node --check ffmpeg.js && node --check processing-queue.js && node --check progress-tracker.js && node --check youtube-audio.js && node --check password-reset.js",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.850.0",
    "@aws-sdk/lib-storage": "^3.850.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "youtube-dl-exec": "^3.1.9"
  }
}
